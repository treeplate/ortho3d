# example

This example uses the Pointer Lock API, in conjunction with ortho3d, to provide a view where you can click to lock the pointer, and then move the mouse to look around the inside of a cube.
